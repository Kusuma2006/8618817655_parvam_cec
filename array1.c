/*print the array elements taking the input from the user*/
#include <stdio.h>
int main()
{
    int size=0,a[100],i,n;
  scanf("%d",&size);
   for(int i=0;i<n;scanf("%d",&a[i++]));
   for(int i=0;i<n;printf("%d",a[i++]));
    return 0;
}